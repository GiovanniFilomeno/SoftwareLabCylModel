function [cylinders] = create_cylinders(polygone_list, z_values)

% Cylinders has to be formatted, as needed by other group

% Use 2D-algorithm:
% [radii,X,Y,X_red,Y_red,radii_red,max_points,min_points,inner_point,k,points] = approximate_by_circles(P,P_end);

end

