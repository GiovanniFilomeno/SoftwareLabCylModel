function [polygone_list, z_values] = define_2D_polygones(mesh_list, z_values)

% polygone_list = [[P,P_end],[P,P_end],...] for each section (as input for
% the function "approximate_by_circles"
% z_values has length number_of_sections + 1 (each section bottom and top)

end

